// §113 tier two — JAPANESE. primer.html's prose, keyed by the English source;
// see src/page-i18n.js for the mechanism and src/primer-i18n.js for this
// page's number rule.
//
// Vocabulary is the standard 時計 trade register, the same one tier one's
// chrome and the explainer use (src/i18n.js, src/explain-i18n.ja.js). The
// going train is numbered from the barrel — 二番車, 三番車, 四番車.
//
// NUMBERS: ja-JP groups with ',' and points with '.', exactly as English does,
// so the digits here READ the same — but they are checked by VALUE, not by
// glyph (see src/primer-i18n.js). The rule is the page's, not the locale's:
// this page quotes no identifiers, so its numbers are quantities.
//
// Landed empty and filled per locale; an untranslated key renders English,
// visibly, which is the honest failure this tier is built around.
export default {
};
